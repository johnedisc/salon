@{
    Layout = "_Layout";
}


@using HairSalon.Models;

<h1>clients</h1>
<hr/>

@if (@Model.Count == 0)
{
  <h3>no clients yet</h3>
  <hr/>
} 
else
{
    @foreach (Client client in Model)
    {
    <div class="content">
      <p>@Html.ActionLink($"{client.Last}, {client.First}", "Details", new { id = client.ClientId }) | @client.Stylist.First</p>
    </div>
    }
}

<h4>@Html.ActionLink("add new client", "Create")</h4>
<h4>@Html.ActionLink("home", "Index", "Home")</h4>


